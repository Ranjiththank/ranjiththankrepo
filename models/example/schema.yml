
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

sources:
  - name: stripe               # How you reference this source: {{ source('stripe', '...') }}
    database: test              # The physical database name in Snowflake/BigQuery/Redshift
    schema: public   # The physical schema name
    tables:
      - name: COUNTRY_STATS
